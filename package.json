{
  "name": "erp",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend/erp && npm install && npm run build && cp -r build ../../backend/build",
    "build": "npm run build:frontend",
    "start:backend": "cd backend && npm install && npm start",
    "start": "npm run start:backend",
    "postinstall": "cd frontend/erp && npm install && cd ../../backend && npm install"
  }
}
