{
  "name": "erp",
  "private": true,
  "scripts": {
    "frontend:build": "cd frontend/erp && npm install && npm run build && cp -r build ../../backend/build",
    "build": "npm run frontend:build",
    "start": "cd backend && npm install && npm start"
  }
}
