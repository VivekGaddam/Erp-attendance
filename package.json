{
  "name": "erp",
  "private": true,
  "scripts": {
    "build": "cd frontend/erp && npm install && npm run build && cp -r frontend/erp/build ../backend/build",
    "start": "cd backend && npm install && npm start"
  }
}
